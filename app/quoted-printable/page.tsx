import Client from "./Client";
export const metadata = {
  title: "Quoted-Printable — HashKitly",
  description: "Encode/decode Quoted-Printable (RFC 2045).",
};
export default function Page() {
  return <Client />;
}
