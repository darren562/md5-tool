import Client from "./Client";
export const metadata = {
  title: "AP Hash — HashKitly",
  description: "Compute AP Hash (non-cryptographic).",
};
export default function Page() {
  return <Client />;
}
