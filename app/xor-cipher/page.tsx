import Client from "./Client";
export const metadata = {
  title: "XOR Cipher — HashKitly",
  description: "XOR cipher (byte-wise), hex/text encode/decode.",
};
export default function Page() {
  return <Client />;
}
