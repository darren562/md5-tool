import Client from "./Client";
export const metadata = {
  title: "JS Hash — HashKitly",
  description: "Compute JS Hash (non-cryptographic).",
};
export default function Page() {
  return <Client />;
}
