import Client from "./Client";
export const metadata = {
  title: "DEK Hash — HashKitly",
  description: "Compute DEK Hash (non-cryptographic).",
};
export default function Page() {
  return <Client />;
}
